package com.example.myapplication.ui.theme

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.ui.unit.dp

val buttonTextPaddings = PaddingValues(vertical = 16.dp)
val firstButtonPaddings = PaddingValues(top = 16.dp)
val commonButtonPaddings = PaddingValues(top = 8.dp)
val buttonContentPaddings = PaddingValues(vertical = 8.dp)

val defaultPadding = 16.dp