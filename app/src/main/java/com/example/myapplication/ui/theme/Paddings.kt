package com.example.myapplication.ui.theme

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.ui.unit.dp

val defaultPadding = 16.dp
val halfDefaultPadding = 8.dp
val bigDefaultPadding = 24.dp

val buttonTextPaddings = PaddingValues(vertical = defaultPadding)
val defaultTopPadding = PaddingValues(top = defaultPadding)
val doubleDefaultTopPadding = PaddingValues(top = defaultPadding * 2)
val halfDefaultTopPadding = PaddingValues(top = defaultPadding / 2)
val mainScreenLazyPadding = PaddingValues(top = 24.dp)
val commonButtonPaddings = PaddingValues(top = 8.dp)
val buttonContentPaddings = PaddingValues(vertical = 12.dp)